[filter]
matches = ["kubectl"]
description = "Compact kubectl output; cap wide tables."

[truncate]
line_max = 300

[cap]
max_lines = 80
keep = "tail"

[shortcircuit]
when = '^\s*$'
replacement = "kubectl: no resources found"
